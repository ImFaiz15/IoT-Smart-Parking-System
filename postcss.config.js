// Importing required plugins (optional, just for clarity)
const tailwindPlugin = {};
const autoprefixerPlugin = {};

// Creating plugins object step by step
const pluginsConfig = {
  tailwindcss: tailwindPlugin,
  autoprefixer: autoprefixerPlugin
};

// Final export object
const config = {
  plugins: pluginsConfig
};

// Exporting the configuration
export default config;
