# ============================================================
# FILE: .env.local   (create this in your project root)
# Copy this file, rename it .env.local, fill in your values.
# ============================================================

# Your backend API base URL (no trailing slash)
# Leave blank to use offline/mock mode (no real backend needed)
VITE_API_URL=http://localhost:8000/api

# Example for production:
# VITE_API_URL=https://your-server.com/api
